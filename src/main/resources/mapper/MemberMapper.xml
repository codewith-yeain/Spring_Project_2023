<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.icontact.mapper.MemberMapper">
    <insert id="insert">
        INSERT INTO TBL_MEMBER
        VALUES(SEQ_MEMBER.NEXTVAL, #{memberEmail}, #{memberPassword}, #{memberName}, #{memberNickname}, #{memberType}, #{memberIntro}, #{memberPhone}, #{memberStatus})
    </insert>

    <select id="selectId" resultType="string">
        SELECT ID FROM TBL_MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail} AND MEMBER_PASSWORD = #{memberPassword}
    </select>

    <select id="selectForEmailCheck" resultType="string">
        SELECT MEMBER_EMAIL FROM TBL_MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail}
    </select>

</mapper>