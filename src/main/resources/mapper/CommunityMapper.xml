<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.icontact.mapper.CommunityMapper">
    <select id="selectMine" resultType="communityVO">
        SELECT ID, COMMUNITY_FILE, COMMUNITY_CONTENT, COMMUNITY_REGISTER_DATE, COMMUNITY_UPDATE_DATE, COMMUNITY_READ_COUNT, COMMUNITY_ANONYM, SORT_ID, USER_ID
        WHERE USER_ID = #{id}
    </select>
</mapper>